# Upgrading

## From v1 to v2

- Install `wotz/laravel-online-scope` instead of `codedor/laravel-online-scope`
- Replace all occurrences of `Codedor\OnlineScope` namespace with new `Wotz\OnlineScope` namespace
